import processing.core.PImage;

import java.util.List;

public class Magikarp extends Obstacle{
    public Magikarp(String id, Point position, List<PImage> images, int animationPeriod) {
        super(id, position, images, animationPeriod);
    }
}
